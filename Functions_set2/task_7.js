/* 7.	Write a function to convert string into an array. Space in a string should be represented as “null” in new array.

"My random string" -> ["M", "y", null, "r", "a"]
"Random" -> ["R", "a", "n", "d", "o", "m"] */

"use strict";
/* if (b === undefined) {
    b ="-";
} 
for() */

var str = "Random";
var arr = [];

function strToArr (str, arr) {
    for (var i = 0; i < str.length; i++)
        if (str[i] === arr[i]){ 
            var newArr = str[str.length[i]];

    }
    return newArr;
}

console.log(newArr(str, arr));
